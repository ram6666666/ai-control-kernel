ACK-IMPL-P01 implementation is developed only on `impl/ack-impl-p01`. Provenance records on `main` document authorization and verification state; they are not implementation code and do not alter frozen v0.1 design/spec/schema/package semantics. The implementation pull request remains subject to clean-checkout CI, independent review, and the frozen `ACK_IMPL_P01_ACCEPTED` gate before merge.
